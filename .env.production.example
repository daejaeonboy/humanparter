# Firebase Hosting rewrite for quote inquiry email delivery.
VITE_SITE_EMAIL_API_URL=/api/email/send

# Optional: used by the company location page to load the Naver Maps JavaScript API.
# VITE_NAVER_MAP_CLIENT_ID=your_naver_maps_client_id

# Optional: only needed if admin features depend on an external API server.
# VITE_API_URL=https://your-api-domain.example.com
