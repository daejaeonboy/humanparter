# Cafe24 static hosting can call the existing Firebase Function directly.
VITE_SITE_EMAIL_API_URL=https://us-central1-human-partner.cloudfunctions.net/sendSiteEmail

# Optional: only needed if admin features depend on an external API server.
# VITE_API_URL=https://your-api-domain.example.com
